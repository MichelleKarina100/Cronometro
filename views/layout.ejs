<!DOCTYPE html>
<html>
<head>
    <meta charset='utf-8'>
    <meta http-equiv='X-UA-Compatible' content='IE=edge'>
    <title>MVC</title>
    <meta name='viewport' content='width=device-width, initial-scale=1'>
    <link href="//maxcdn.bootstrapcdn.com/bootstrap/4.1.1/css/bootstrap.min.css" rel="stylesheet">
    <link href="/css/styles.css" rel="stylesheet">
    <script src="//maxcdn.bootstrapcdn.com/bootstrap/4.1.1/js/bootstrap.min.js"></script>
    <script src="/js/script.js" ></script>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.3.1/jquery.min.js"></script>

    
    <script>
        $(document).ready(function(){
            $("#parar").click(function(){  
              console.log("entra");
              //Get value hours
              var horas = $("#Horas").text();
              //Get value minutes
              var minutos = $("#Minutos").text();
              //Get value seconds
              var segundos = $("#Segundos").text();
              //Get value centesimas
              var centesimas = $("#Centesimas").text();
              console.log(horas, minutos, segundos, centesimas);
              // $.ajax({
              //   url: "http://localhost:4111/cronometro", 
              //   data:{'auth':'465dsfsd5f44654'}, 
              //   type: "POST", 
              //   headers: { 'Content-Type': 'application/json' }, 
              //   success: function (data) { } 
              // });
              $.post("http://localhost:4111/cronometro",
              {
                horas,
                minutos,
                segundos,
                centesimas
              },
              function(data,status){
                  alert("Data: " + data + "\nStatus: " + status);
              })
              .fail(function() {
                alert( "error" );                  //EN EL NAVEGADOR SE EJECUTA ESTE CODIGO DE ERROR
              });


            });
        });
    </script>
</head>
<body><!-- Just an image -->
    <!-- <nav class="navbar navbar-light bg-light">
      <a class="navbar-brand" href="#">
        <img src="https://getbootstrap.com/docs/4.0/assets/brand/bootstrap-solid.svg" width="30" height="30" alt="">
      </a>
    </nav> -->